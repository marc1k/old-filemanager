[package]
name = "opal-lib"
version = "0.1.0"
authors = ["marc1 <m@marc.rs>"]
edition = "2018"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
opal-tui = { version = "0.1.0", path = "../tui" }
